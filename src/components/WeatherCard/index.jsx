import "./index.css";

const WeatherCard = ({ data }) => {
  console.log(data);
  return (
    <div className="weatherCardComponent">
      <h1>fromWeatherCard</h1>
    </div>
  );
};

export default WeatherCard;
